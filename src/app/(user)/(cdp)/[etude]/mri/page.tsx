import { Metadata } from 'next';

export const metadata = {
    title: 'Écriture MRI',
} satisfies Metadata;

export default () => {
    return (
        <div>
            <div>Hello World</div>
        </div>
    );
};
